using System;
using Xunit;
using Global.Logger;
using Logger.DbLog;

namespace Global.Logger.Tests
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
          //  var provider = new LoggerProvider(null);
          //  var logger = provider.CreateLogger("any");
          //  Assert.True(logger is CustomLogger);
        }
    }
}
